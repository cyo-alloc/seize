[package]
name = "cortex-m-qemu"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
seize = { path = "../..", default-features = false }
cortex-m-rt = "0.7"
cortex-m-semihosting = "0.5"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[workspace]
